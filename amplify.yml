version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd apps/website
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: apps/website/.next
    files:
      - '**/*'
  cache:
    paths:
      - apps/website/node_modules/**/*
