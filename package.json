{
  "name": "golden-age-learning",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "functions",
    "shared"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=website",
    "build": "npm run build --workspace=website",
    "start": "npm run start --workspace=website",
    "lint": "npm run lint --workspace=website"
  }
}
